import generateSeoMetadata from "@/utils/seo/generateSeoMetadata"

const installationMetadata = generateSeoMetadata({
    title: "Installation Steps for Rad UI",
    description: "Installation steps for Rad UI - an Open Source React Headless UI Library",
})

export default installationMetadata