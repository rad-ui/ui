import generateSeoMetadata from '@/utils/seo/generateSeoMetadata'

const hiddenInputMetadata = generateSeoMetadata({
  title: 'Hidden input strategies | Rad UI',
  description: 'Submit Rad UI composite controls through native HTML forms with hidden inputs.'
})

export default hiddenInputMetadata
