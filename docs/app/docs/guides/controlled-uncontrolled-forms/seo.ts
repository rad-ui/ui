import generateSeoMetadata from '@/utils/seo/generateSeoMetadata'

const controlledFormsMetadata = generateSeoMetadata({
  title: 'Controlled vs uncontrolled forms | Rad UI',
  description: 'When to use controlled or uncontrolled state with Rad UI form primitives.'
})

export default controlledFormsMetadata
