import generateSeoMetadata from '@/utils/seo/generateSeoMetadata'

const keyboardSpecMetadata = generateSeoMetadata({
  title: 'Keyboard interaction spec | Rad UI',
  description: 'Template and pattern map for documenting keyboard behavior in Rad UI components.'
})

export default keyboardSpecMetadata
