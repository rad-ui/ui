import generateSeoMetadata from '@/utils/seo/generateSeoMetadata'

const browserSupportMetadata = generateSeoMetadata({
  title: 'Browser Support | Rad UI',
  description: 'Supported browsers, compatibility assumptions, and validation policy for Rad UI.'
})

export default browserSupportMetadata
