import Documentation from '@/components/layout/Documentation/Documentation';
import DialogExample from "./docs/example_1"
import {code, anatomy, api_documentation} from "./docs/codeUsage"

<Documentation
    title={`Dialog`}
    description='A dialog that is used to display a message to the user. It is used to display information and let the user interact with the content.'
>

    {/* Component Hero */}
    <Documentation.ComponentHero codeUsage={code}>
        <DialogExample />
    </Documentation.ComponentHero>

    <Documentation.ApiReference
        anatomy={anatomy.code}
        tables={api_documentation}
        order={['root', 'trigger', 'portal', 'overlay', 'content', 'title', 'description', 'footer', 'close']}
    />

</Documentation>
