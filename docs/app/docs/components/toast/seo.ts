import generateSeoMetadata from '@/utils/seo/generateSeoMetadata'

const toastMetadata = generateSeoMetadata({
    title: 'Toast - Rad UI',
    description:
        'A headless, composable toast notification system for React. Stack, position, and style notifications with swipe-to-dismiss, promises, and an imperative manager API.',
})

export default toastMetadata
